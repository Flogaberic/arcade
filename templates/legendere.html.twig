{% extends "base.html.twig" %}

{% block title %} {{ nom }} | {{ parent() }} {% endblock %}

{% block body %}

<div class="presentation">
    <h1>{{ nom }}</h1>
    <h5>{{ categorie }}</h5>
    <h5>{{ highscore }}</h5>
    <img src="{{ img }}" width="500px"><br>
    <a href="{{ chemin }}"><button class="btn btn-primary">Jouer</button></a>
</div>

{% endblock %}